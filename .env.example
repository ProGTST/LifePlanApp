# ブラウザで public/data の CSV を読み書きする場合に API サーバーを指す。
# 本番では .env にコピーし、VITE_DATA_API_BASE を設定する（例: http://localhost:3000）。
# 未設定のときは Tauri 時は invoke、ブラウザ時は保存不可。
# VITE_DATA_API_BASE=http://localhost:3000
